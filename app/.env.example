# Required. Without it the app reads nothing and says so — it never
# falls back to a guessed address.
NEXT_PUBLIC_CONTRACT_ADDRESS=0xd72f403b40EB84BE4f8b5024BfA394BFa19E1c5a

# Optional.
NEXT_PUBLIC_DEPLOY_ENV=studionet
# NEXT_PUBLIC_GENLAYER_RPC_URL=
# NEXT_PUBLIC_EXPLORER_URL=
# Enables WalletConnect. Injected wallets work without it.
# NEXT_PUBLIC_WALLETCONNECT_PROJECT_ID=
